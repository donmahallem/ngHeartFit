mat-slide-toggle([checked]="isSelected",[disabled]="!isValidFile",labelPosition="after",(change)="onChangeSelection($event)")
h3 {{uploadFile?.filename}}
app-file-upload-progress(*ngIf="uploadFile?.status===2")
app-file-upload-error(*ngIf="uploadFile?.status===1")